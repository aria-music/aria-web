pull_request_rules:
  - name: merge when build passes
    conditions:
      - author=@web
      - status-success=build
      - label!=do not merge
    actions:
      merge:
        method: squash
        strict: true
      delete_head_branch:
